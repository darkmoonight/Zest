/// Canonical route paths used by [GoRouter] and navigation helpers.
abstract final class AppRoutes {
  /// Root route for the main shell.
  static const home = '/';

  /// Onboarding flow shown before first use.
  static const onboarding = '/onboarding';
}
