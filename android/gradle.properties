org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
android.enableR8.fullMode = false
# Keep legacy KGP until plugins (e.g. device_calendar_plus_android) migrate to built-in Kotlin.
# restart_app is pinned below 1.9.0 for the same reason (1.9.0 assumes builtInKotlin=true).
android.builtInKotlin=false
android.newDsl=false
